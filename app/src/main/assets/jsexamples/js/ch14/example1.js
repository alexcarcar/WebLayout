$(document).ready(function() {
	'use strict';
	const log = console.log;
	/*
	log("---------------------------------");
	log("Before timeout: " + new Date());
	function f() {
		log("After timeout: " + new Date());
	}
	setTimeout(f, 2*1000); // 5 seconds
	log("I happened after setTimeout!");
	log("Me too!"); 
	log("---------------------------------");
	const start = new Date();
	let i=0;
	const intervalId = setInterval(function(){
		let now = new Date();
		if(now.getMinutes()!==start.getMinutes()||++i>10)
			return clearInterval(intervalId);
		log(`${i}: ${now}`);
	}, 5*1000);
	*/
	log("---------------------------------");
	log("---------------------------------");
	log("---------------------------------");
	log("---------------------------------");
	log("---------------------------------");
	log("---------------------------------");

});